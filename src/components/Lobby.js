import React from 'react'

const Lobby = () => {
  return(
    <div
      id="lobby"
      className='floor-container'
      style={{backgroundColor: 'white'}}>
    </div>
  )
}

export default Lobby
